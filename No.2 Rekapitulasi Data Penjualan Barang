#include <iostream>

using namespace std;

 struct penjualan_barang{
     string nama;
    int data_barang_terjual, total; 
     const int item1= 100000;
    const int  item2=90000;
    const int  item=80000;
     const int item4=70000;
    const int  item5=60000;
      const int item6=50000;
      const int item7=40000;
     const int item8=30000;
    const int  item9=20000;
     const int aqua=10000;
 };
 int main(){
     Nama barang[3];
     string laku;
     int maximal=0;
     int n=3, total_penjualan=0;
     
     for (int i=0;i<n;i++){
         cout<<"Nama Item Baramg:";
         cin>>item[i].nama;
         if(item[i].nama=="item1"){
             item[i].harga=item[i].item1;
         }else if(item[i].nama=="item2"){
             item[i].harga=item[i].item2;
         }else if (item[i].nama=="item3"){
             item[i].harga=item[i].item3;
         }else if (item[i].nama=="item4"){
             item[i].harga=item[i].item4;
         }else if (item[i].nama=="item5"){
             item[i].harga=item[i].item5;
         }else if (item[i].nama=="item6"){
         item[i].harga=item[i].item6;
         }else if (item[i].harga=="item7"){
             item[i].harga=item[i].item7;
         }else if (item[i].harga=="item8"){
             item[i].harga=item[i].item8;
         }else if (item[i].harga=="item9"){
             item[i].harga=item[i].item9;
         }
         cout<<"data barang terjual:";
         cin<<item[i].data_barang_terjual;
         item[i].total=
         item[i].harga*item[i].data_barang_terjual;
     }
     for(int i=0;i<n;i++){
         total_penjual=total_penjualan+item[i].total;
     }
     for (int i=0;i<n;i++){
         if(maximal<item[i].jumlah_jual){
             maximal=item[i].jumlah_jual;
             laku=item[i].nama;
             
         }
     }
       cout <<endl;
    cout <<endl;
    cout << "Nama item barang | Harga per item | jumlah terjual | total per item"<< endl;
    for(int i=0; i < 3; i++){
        cout << item[i].nama <<"                " << item[i].harga << "           "<< item[i].jum_jual<< "                " << item[i].total <<endl;
    }
    
    cout << endl;
    cout << "Total penjualan : " << tot_pen << endl;
    cout << "Item yang paling laku adalah " << laku << endl;
}
